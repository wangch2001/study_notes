# 推荐系统公开课笔记

# 概要

## 小红书推荐系统

有效点击：在内容界面进行停留

### 转化流程

![image-20250531200154066](./assets/image-20250531200154066.png)

B站、淘宝、快手、知乎等等都有**曝光和点击**，但抖音没有，因为抖音下滑只能看到一个作品

滑动到底、点赞、收藏、转发可以作为推荐的依据

评论则可以塑造更加良好的社区氛围

### 消费指标

![image-20250531200455341](./assets/image-20250531200455341.png)

我们希望新的算法能够超越原有算法

这些都是短期消费指标，不是最重要的指标

### 北极星指标

衡量推荐系统好坏的主要指标

![image-20250531200801826](./assets/image-20250531200801826.png)

如果消费指标与北极星指标出现冲突，以北极星指标为准

### 实验流程

![image-20250531201149184](./assets/image-20250531201149184.png)

## 推荐系统链路

![image-20250531201250418](./assets/image-20250531201250418.png)

### 召回

![image-20250531201542424](./assets/image-20250531201542424.png)

推荐系统会融合这些笔记，然后做去重和过滤，去掉重复内容，过滤掉不喜欢的作者、内容、话题

### 排序：

![image-20250531201940524](./assets/image-20250531201940524.png)

精排之后可以按照评分返回给用户，但此时结果仍存在不足，需要进行重排，重排主要考虑多样性，根据多样性进行随机抽样，从几百篇中选出几十篇，同时将内容相似笔记打散，即可将内容展现给用户。

### 粗排、精排：

![image-20250531202106722](./assets/image-20250531202106722.png)

### 重排：

![image-20250531202154604](./assets/image-20250531202154604.png)

### 总结

![image-20250531202239029](./assets/image-20250531202239029.png)

## A/B测试

A/B测试是什么？（例如）

+ 召回团队实现了一种GNN召回通道离线实验结果正向
+ 下一步是做线上的小流量A/B测试，考察新的召回通道对线上指标的影响。
+ 模型中有一些参数，比如GNN的深度取值∈{1,2,3}，需要用A/B测试选取最优参数。

意义：

+ 做线上小流量A/B测试，考察新方法对线上指标的影响
+ 通过A/B测试，选取最优参数
+ 实验组指标显著优于对照组，则值得推全

### 随机分桶

使用哈希函数将用户ID进行映射，分为不同的桶（也就是随机划分）

每一组应用不同的策略，测试新策略是否有效，设置对照组进行对比

### 分层实验

分层实验的目标是解决流量不够用的问题

+ **分层实验：**召回、粗排、精排、重排、用户界面、广告(例如GNN召回通道属于召回层。)
+ **同层互斥：**GNN实验占了召回层的4个桶，其他召回实验只能用剩余的6个桶
+ **不同层正交：**每一层独立随机对用户做分桶。每一层都可以独立用100%的用户做实验。

![image-20250601150121312](./assets/image-20250601150121312.png)

+ 同类的策略天然互斥，可能互相干扰，对于一个用户只能用其中一种。
+ 不同类型的策略通常不会相互干扰，可以正交。

### Holdout机制

+ 每个实验（召回、粗排、精排、重排)独立汇报对业务指标的提升
+ 公司考察一个部门（比如推荐系统)在一段时间内对业务指标总体的提升 
+ 取10%的用户作为holdout桶，推荐系统使用剩余 90%的用户做实验，两者互斥 
+ 10%holdout桶vs90%实验桶的diff（需要归一化）为整个部门的业务指标收益

 ![image-20250601151318573](./assets/image-20250601151318573.png)

+ ·每个若核周期结束之后，清除holdout桶，让推全实验从90%用户扩大到100%用户
+ 重新随机划分用户，得到holdout桶和实验桶，开始下一轮考核周期。
+ 新的holdout桶与实验桶各种业务指标的diff接近0。
+ 随着召回、粗排、精排、重排实验上线和推全，diff会逐渐扩大。

### 实验推全

![image-20250601152114519](./assets/image-20250601152114519.png)

### 反转实验

+ 有的指标（点击、交互)立刻收到新策略影响，有的指标（留存)有滞后性，需要长期观测
+ 实验观测到显著收益后尽快推全新策略。目的是腾出桶供其他实验使用，或需要基于新策略做后续的开发
+ 用反转实验解决上述矛盾，既可以尽快推全，也可以长期观测实验指标
+ 在推全的新层中开一个旧策略的桶，长期观测实验指标

![image-20250601151822296](./assets/image-20250601151822296.png)

### 总结

+ **分层实验：**同层互斥（不允许两个实验同时影响一位用户)、不同层正交（实验有重叠的用户） 
+ **Holdout：**保留10%的用户，完全不受实验影响，可以考察整个部门对业务指标的贡献。
+ **实验推全：**新建一个推全层，与其他层正交。
+ **反转实验：**在新的推全层上，保留一个小的反转桶，使用旧策略。长期观测新旧策略的dff

# 召回

## 基于物品的协同过滤（ItemCF)

根据用户的喜好和其它兴趣相近的用户推荐物品

应用最广泛的：基于邻域的方法

+ UserCF：推荐相似用户喜欢的东西
+ ItemCF：推荐他人之前喜欢的物品相似的物品

**常用相似度**

+ Jaccard相关系数
+ 余弦相似度
+ 皮尔逊相关系数，相比于余弦相似度，通过用户平均分来减小偏置影响

**召回率：**预测准确的物品占用户实际喜欢物品的比例

**精确率：**对用户准确推荐的物品占总推荐物品的比例

**覆盖率：**推荐系统能够推荐出来的物品占总物品集合的比例

**协同过滤的缺点**

+ 泛化能力弱，无法将两个物品相似的信息推广到其它物品的相似性上
+ 热门物品具有很强的头部效应，容易跟大量物品产生相似，而尾部物品由于特征向量稀疏，导致很少被推荐

**什么时候用UserCF，什么时候用ItemCF，为什么？**

1. UserCF：所以具备更强的社交特性，这样的特点非常适于用户少物品多，时效性较强的场合。比如新闻推荐场景。此外，UserCF可以发现用户潜在但自己尚未察觉的兴趣爱好。
2. ItemCF：适用于兴趣变化较为稳定的应用，适合物品少，用户多，用户兴趣固定持久的场合。比如推荐音乐、电影。

## Swing算法

之前的Item-based CF只关注于ltem-User-ltem的路径，即通过物品与用户的交互，来推断物品与物品之间的相似度，从而进行推荐。忽视了噪声，推荐精度存在局限性。

 Swing通过利用User-ltem-User路径中所包含的信息进行推荐：

若用户u和用户V之间除了购买过ⅰ外，还购买过商品j，则认为两件商品是具有某种程度上的相似的。也就是说，商品与商品之间的相似关系，是通过用户关系来传递的如果这两个用户共同购买的物品越少，即这两个用户原始兴趣不相以，但仍同时购买了两个相同的物品，则物品和的相以性越高。

![image-20250604094949177](./assets/image-20250604094949177.png)

## 矩阵补充

+ 把物品ID、用户ID做embedding,映射成向量。
+ 两个向量的內积 < $a_u$ , $b_i$ > 作为用户u对物品i兴趣的预估。
+ 让 < $a_u$ , $b_i$ > 拟合真实观测的兴趣分数，学习模型的 embedding层参数。
+ 矩阵补充模型有很多缺点，效果不好。

缺点1：仅用ID embedding，没利用物品、用户信息

+ 物品属性：类目、关键词、地理位置、作者信息
+ 用户属性：性别、年龄、地理位置、感兴趣的类目
+ 双塔模型可以看做矩阵补充的升级版

缺点2：负样本的选取方式不对

+ 样本：用户--物品的二元组，记作（u，i）
+ 正样本：曝光之后，有点击、交互（正确的做法）
+ 负样本：曝光之后，没有点击、交互（错误的做法）

缺点3：做训练的方法不好

+ 内积 < $a_u$ , $b_i$ > 不如余弦相似度
+ 用平方损失（回归），不如用交叉熵损失（分类）

## 双塔模型

其中ID和离散特征用Embedding层处理，连续特征用归一化或分桶做处理

用户表征：

![image-20250604101009248](./assets/image-20250604101009248.png)

物品表征：

![image-20250604101015395](./assets/image-20250604101015395.png)

双塔模型：

![image-20250604101112962](./assets/image-20250604101112962.png)

**训练方式：**

1. Pointwise：独立看待每个正样本、负样本，做简单的二元分类

   + 把召回看作二分类任务
   + 对于正样本，鼓励 cos(a, b) 接近+1
   + 对于负样本，鼓励 cos(a, b) 接近-1
   + 正负样本数量控制在1：2或1：3

2. Pairwise：每次取一个正样本、一个负样本

   + 每次输入三元组 <用户a， 正样本b+，负样本b->

   + 基本想法：鼓励 cos(a, b+) 大于 cos(a, b-)

   + 超参数m，正负样本的兴趣之差

   + ![image-20250604102537734](./assets/image-20250604102537734.png)

   + 可以通过最小化以下损失函数来训练双塔模型

     ![image-20250604101751993](./assets/image-20250604101751993.png)

3. Listwise：每次取一个正样本、多个负样本

   + 每次输入一个用户，一个正样本，多个负样本
   + 同样是鼓励 cos(a, b+) 尽量大，cos(a, b-) 尽量小
   + 将 cos(a, b+) 和所有cos(a, b-) 输入softmax进行归一化
   + ![image-20250604102755269](./assets/image-20250604102755269.png)
   + 利用交叉熵做损失函数，训练时最小化交叉熵，使得s+接近y+=1，s-接近y-=0

+ 用户塔、物品塔各输出一个向量。
+ 两个向量的余弦相似度作为兴趣的预估值。
+ 三种训练方式：
  + Pointwise：每次用一个用户、一个物品（可正可负)
  + Pairwise：每次用一个用户、一个正样本、一个负样本
  + Listwise：每次用一个用户、一个正样本、多个负样本

下图属于前期融合，属于排序模型，不适用做召回

![image-20250604103050040](./assets/image-20250604103050040.png)

## 双塔模型：正负样本

**正样本：**

+ 曝光且有点击的用户物品二元组
+ 问题：少部分物品占据大量点击导致正样本多是热门物品
+ 解决方法：对冷门物品过采样（采样多次），对热门物品降采样（抛弃一些样本）

**负样本：**

+ 简单负样本

  + 全体物品

    均匀抽样？

    对冷门不公平-因为正样本大多是热门物品，导致负样本中热门物品占比少

    解决方法：非均匀抽样一物品抽样的概率与热门程度正相关

    抽样概率∝点击次数0.75

    使得热门物品成为负样本的概率大

  + batch内负样本

    ![image-20250604104618263](./assets/image-20250604104618263.png)

    但这种负样本中，抽样概率∝点击次数，热门物品成为负样本的概率过大

    解决方法：将用户的兴趣调整为 $cos(\mathrm{a,b_i)-}\log p_i$

    其中 $p_i$ ∝点击次数

+ 困难负样本

  1. 被粗排淘汰的物品（比较困难）
  2. 精排分数靠后的物品（非常困难）

+ 对正负样本做二元分类

  + 全体物品（简单）分类准确率高
  + 被粗排淘汰的物品（比较困难）容易分错
  + 精排分数靠后的物品（非常困难）更容易出错

+ 工业常用做法

  + 混合几种负样本
  + 50%的负样本是全体物品（简单负样本)
  + 50%的负样本是没通过排序的物品（困难负样本)

![image-20250604105409447](./assets/image-20250604105409447.png)

**总结**

+ 正样本：曝光而且有点击
+ 简单负样本：
  + 全体物品
  + batch内负样本
+ 困难负样本：被召回，但是被排序淘汰
+ 错误：曝光、但是未点击的物品做召回的负样本

## 双塔模型：线上服务与模型更新

**离线存储：**把物品向量b存入向量数据库

1. 完成训练之后，用物品塔计算每个物品的特征向量b
2. 把几亿个物品向量b存入向量数据库（比如Milvus Faiss、HnswLib)
3. 向量数据库建索引，以便加速最近邻查找。

**线上召回：**用户塔不事先计算和存储用户向量，而是当用户发起推荐请求的时候，调用神经网络在线上线算一个特征向量a  

1. 给定用户ID和画像，线上用神经网络算用户向量a
2. 最近邻查找：
   + 把向量a作为query，调用向量数据库做最近邻查找
   + 返回余弦相似度最大的k个物品，作为召回结果。

事先存储物品向量b,线上现算用户向量a,why?

+ 每做一次召回，用到一个用户向量a,几亿物品向量b。(线上算物品向量的代价过大。)
+ 用户兴趣动态变化’而物品特征相对稳定。（可以离线存储用户向量，但不利于推荐效果。)

**全量更新：**今天凌晨，用昨天全天的数据训练模型。

+ 在昨天模型参数的基础上做训练。（不是随机初始化)
+ 用昨天的数据，训练1 epoch，即每天数据只用一遍。
+ 发布新的用户塔神经网络和物品向量，供线上召回使用。
+ 全量更新对数据流、系统的要求较低。

**增量更新：**做online learning更新模型参数。

+ 用户兴趣会随时发生变化。
+ 实时收集线上数据，做流式处理，生成TFRecord文件
+ 对模型做online learning，增量更新ID Embedding参数。(不更新神经网络其他部分的参数。)
+ 发布用户ID Embedding，供用户塔在线上计算用户向量

![image-20250604151121032](./assets/image-20250604151121032.png)

问题：能否只做增量更新，不做全量更新？

+ 小时级数据有偏；分钟级数据偏差更大
+ 全量更新：random shuffle一天的数据，做1 epoch训练
+ 增量更新：按照数据从早到晚的顺序，做1 epoch训练
+ 随机打乱优于按顺序排列数据，全量训练优于增量训练

全量训练的模型更好，而增量训练可以实时捕捉用户的兴趣。  

而增量训练中，小时/分钟级别的数据有偏，与全天的数据差别很大。因此，不能只增量训练，而做全量训练的时候也要随机排列数据  

## 双塔模型+自监督学习

自监督的目的是把物品塔训练的更好

**双塔模型的问题：**

+ 推荐系统的头部效应严重：
  + 少部分物品占据大部分点击。
  + 大部分物品的点击次数不高。
+ 高点击物品的表征学得好，长尾物品的表征学得不好
+ 自监督学习：做data augmentation，更好地学习长尾物品的向量表征。

**双塔模型的训练：**

双塔模型采用Listwise进行训练

![image-20250604152618579](./assets/image-20250604152618579.png)

纠偏：

![image-20250604152650421](./assets/image-20250604152650421.png)

![image-20250604152139517](./assets/image-20250604152139517.png)

**自监督学习：**

训练物品塔

![image-20250604152804774](./assets/image-20250604152804774.png)

![image-20250604152829720](./assets/image-20250604152829720.png)

+ 物品i的两个向量表征 $\mathbf{b}_i^{\prime}$ 和 $\mathbf{b}_i^{\prime\prime}$ 有较高的相似度。
+ 物品i和j的向量表征 $\mathbf{b}_i^{\prime}$ 和 $\mathbf{b}_i^{\prime\prime}$ 有较低的相似度。
+ 鼓励cos( $\mathbf{b}_i^{\prime}$ , $\mathbf{b}_i^{\prime\prime}$ )尽量大cos( $\mathbf{b}_i^{\prime}$ , $\mathbf{b}_i^{\prime\prime}$ )尽量小。

**特征变换：Random Mask**

+ 随机选一些离散特征（比如类目)，把它们遮住。
+ 例：某物品的类目特征是U={数码，摄影}，Mask后的类目特征是U'={default}

**特征变换：Dropout（仅对多值离散特征生效)**

+ 一个物品可以有多个类目，那么类目是一个多值离散特征。
+  Dropout：随机丢弃特征中50%的值。
+ 例：某物品的类目特征是U={美妆，摄影}。 Dropout后的类目特征是U'={美妆}

**特征变换：互补特征（complementary)**

+ 假设物品一共有4种特征： ID,类目，关键词，城市
+ 随机分成两组：{ID,关键词}和{类目，城市}
+ {ID,default,关键词，default}→物品表征
+ {default,类目，default,城市}→物品表征
+ 由于是对同一物体的表征，因此这两个物品表征应该相似

**特征变换：Mask一组关联的特征**

+ 受众性别：U={男，女，中性}

+ 类目：v={美妆，数码，足球，摄影，科技，… }

+ u=女和v=美妆同时出现的概率p(u,v)大

+ u=女和v=数码同时出现的概率p(u,v)小。

+ p(u)：某特征取值为u的概率。 

+ p(u,v)：某特征取值为u，另一个特征取值为v，同时发生的概率。

+ 离线计算特征两两之间的关联，用互信息(mutual information)衡量：

  $MI(\mathcal{U},\mathcal{V})=\Sigma_{u\in\mathcal{U}}\Sigma_{v\in\mathcal{V}}p(u,v)\cdot\log\frac{p(u,v)}{p(u)\cdot p(v)}.$

+ 设一共有k种特征。离线计算特征两两之间MI,得到kXk的矩阵

+ 随机选一个特征作为种子，找到种子最相关的k/2种特征

+ Mαsk种子及其相关的k/2种特征，保留其余的k/2种特征

+ 好处：比random mask、dropout、互补特征等方法效果更好。

+ 坏处：方法复杂，实现的难度大，不容易维护。

![image-20250604154218288](./assets/image-20250604154218288.png)

 ![image-20250604154329583](./assets/image-20250604154329583.png)

![image-20250604154354335](./assets/image-20250604154354335.png)

![image-20250604154530943](./assets/image-20250604154530943.png)

![image-20250604154514686](./assets/image-20250604154514686.png)

## Deep Retrieval召回

+ 经典的双塔模型把用户、物品表示为向量，线上做最近邻查找。 
+ Deep Retrieval把物品表征为路径（path),线上查找用户最匹配的路径
+ Deep Retrieval类似于阿里的TDM

## 其它召回通道

**地理位置召回：**

**GeoHash召回**

+ 用户可能对附近发生的事感兴趣
+  GeoHash：对经纬度的编码，地图上一个长方形区域
+ 索引：GeoHash→优质笔记列表（按时间倒排) 
+ 这条召回通道没有个性化

**同城召回**

+ 用户可能对同城发生的事感兴趣
+ 索引：城市〉优质笔记列表（按时间倒排）
+ 这条召回通道没有个性化

**作者召回：**

+ 关注作者：用户-关注的作者-最新的笔记
+ 交互作者：用户-有交互的作者-最新的笔记
+ 相似作者：用户-感兴趣作者-相似的作者最新的笔记

**缓存召回：**

+ 复用前n次推荐精排的结果
+ 精排前50但是没有曝光的缓存起来，作为召回通道
+ 退场机制：被曝光，召回次数或缓存时间达标

![image-20250604163025365](./assets/image-20250604163025365.png)

## 曝光过滤&Boom Filter

曝光过滤：不曝光用户已浏览的物品

**Bloom Filter：**

+ Bloom filter判断一个物品ID是否在已曝光的物品集合中
+ 如果判断为no，那么该物品一定不在集合中。
+ 如果判断为yes那么该物品很可能在集合中。（可能误伤，错误判断未曝光物品为已曝光，将其过滤掉。)

![image-20250604163611214](./assets/image-20250604163611214.png)

**误伤概率的三个影响因素：**

+ 物品集合n
+ 二进制向量维度m
+ 哈希函数的个数k

![image-20250604163846262](./assets/image-20250604163846262.png)

**Bloom Filter的缺点：**

+ Bloom filter把物品的集合表示成一个二进制向量。
+ 每往集合中添加一个物品，只需要把向量k个位置的元素置为1。（如果原本就是1，则不变。) 
+ Bloom filter只支持添加物品，不支持删除物品。从集合中移除物品，无法消除它对向量的影响。
+ 每天都需要从物品集合中移除年龄大于1个月的物品。（超龄物品不可能被召回，没必要把它们记录在 Bloom filter，降低n可以降低误伤率。)

# 排序























































































































































